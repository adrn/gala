version: 2

build:
  image: latest

conda:
  environment: .rtd-environment.yml

python:
  version: 3.11
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs

formats: []
